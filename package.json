{
  "name": "@project-rouge/rg-changeset-action",
  "version": "0.0.0",
  "repository": "https://github.com/Project-Rouge/rg-changeset-action",
  "author": "modulous",
  "license": "UNLICENSED",
  "description": "Github action to handle changesets in rg-suite",
  "main": "dist/main.js",
  "scripts": {
    "build:local": "esbuild src/main.ts --platform=node --target=node16 --outfile=dist/main.mjs",
    "build": "esbuild src/main.ts --bundle --platform=node --target=node16 --outfile=dist/main.js"
  },
  "dependencies": {
    "@actions/exec": "^1.1.1",
    "@actions/github": "^5.1.1",
    "dotenv": "^16.0.3"
  },
  "devDependencies": {
    "@types/node": "^18.11.9",
    "esbuild": "^0.15.15"
  }
}
